"$schema": "https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json"
name: Slider
group: Empire
status: stable
description: "A horizontal streaming-style rail of items with snap scrolling. Keyboard scrollable."
props:
  type: object
  properties:
    title:
      type: string
      title: Title
      description: "Optional heading shown above the rail (e.g. a collection name)."
      default: ""
slots:
  items:
    title: Items
