{# Empire badge — small label. #}
{% if text is not empty %}<span{{ (attributes ?? create_attribute()).addClass('empire-badge', 'empire-badge--' ~ variant|default('accent')) }}>{{ text }}</span>{% endif %}
