langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_source_status
    - node.type.empire_video
  module:
    - options
id: node.empire_video.field_source_status
field_name: field_source_status
entity_type: node
bundle: empire_video
label: 'Source status'
description: 'Active shows the video normally. Hidden keeps the record but removes it from every listing. Unavailable replaces the player with a notice (use when removed or made private on YouTube).'
required: false
translatable: true
default_value:
  -
    value: active
default_value_callback: ''
settings: {  }
field_type: list_string

